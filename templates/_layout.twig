<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{% block title %}{{ siteName }}{% endblock %}</title>
  <style>
    * { box-sizing: border-box; }
    body { margin: 0; font-family: -apple-system, system-ui, sans-serif; background: #f7f7f7; color: #1b1b1b; }
    header, footer { padding: 12px 18px; }
    header {
      display: flex;
      justify-content: center;
      align-items: center;
      border-bottom: 1px solid #e5e5e5;
    }
    header .logo {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-weight: 700;
      color: #111;
      text-decoration: none;
    }
    header .dot { width: 10px; height: 10px; border-radius: 50%; background: #111; display: inline-block; }
    @media (min-width: 768px) { header { justify-content: flex-start; } }
    footer { border-top: 1px solid #e5e5e5; }
  </style>
  {% block styles %}{% endblock %}
</head>
<body>
  {% set renderHeader = renderHeader is defined ? renderHeader : true %}
  {% set renderFooter = renderFooter is defined ? renderFooter : true %}

  {% if renderHeader %}
    <header>
      <a class="logo" href="{{ siteUrl }}">
        <span class="dot"></span>
        <span>Recipes</span>
      </a>
    </header>
  {% endif %}

  {% block content %}{% endblock %}

  {% if renderFooter %}
    <footer>
      <div style="color:#666; font-size:14px; padding: 0 18px;">© {{ "now"|date("Y") }} Recipes</div>
    </footer>
  {% endif %}
</body>
</html>
