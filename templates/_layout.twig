<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{% block title %}{{ siteName }}{% endblock %}</title>
  <link rel="icon" href="/favicon.ico">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="manifest" href="/site.webmanifest">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Newsreader:opsz,wght@6..72,300..700&display=swap" rel="stylesheet">
  <style>
    * { box-sizing: border-box; }
    body { margin: 0; min-height: 100vh; display: flex; flex-direction: column; font-family: "Newsreader", ui-serif, Georgia, serif; background: #fbf5e6; color: #1b1b1b; }
    header, footer { padding: 12px 18px; }
    header {
      display: flex;
      justify-content: center;
      align-items: center;
      border-bottom: 1px solid #e5e5e5;
    }
    header .logo {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-weight: 700;
      color: #111;
      text-decoration: none;
    }
    header .dot { width: 18px; height: 18px; display: inline-block; background: url('/favicon.svg') center / contain no-repeat; }
    footer { margin-top: auto; border-top: 1px solid #e5e5e5; text-align: center; }
  </style>
  {% block styles %}{% endblock %}
</head>
<body>
  {% set renderHeader = renderHeader is defined ? renderHeader : true %}
  {% set renderFooter = renderFooter is defined ? renderFooter : true %}

  {% if renderHeader %}
    <header>
      <a class="logo" href="/">
        <span class="dot"></span>
        <span>Potatos</span>
      </a>
    </header>
  {% endif %}

  {% block content %}{% endblock %}

  {% if renderFooter %}
    <footer>
      <div style="color:#666; font-size:14px; padding: 0 18px;">© {{ "now"|date("Y") }} Potatoes</div>
    </footer>
  {% endif %}
</body>
</html>
